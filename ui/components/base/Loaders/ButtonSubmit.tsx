import { FunctionComponent } from 'react';

import ReactLoading from 'react-loading';

export const ButtonSubmit: FunctionComponent = () => (
    <ReactLoading type={'spinningBubbles'} color={'white'} height={18} width={18} />
);
